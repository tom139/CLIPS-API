'use strict';

module.exports.db = {
    client: 'mysql',

    connection: {
        'host': 'localhost',
        'user': 'root',
        'password': 'pass',
        'database': 'CLIPS'
    }
};

module.exports.proofPath = "../proofs/";
